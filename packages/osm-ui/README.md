# `@osm-editor-kit/osm-ui`

**Status:** Private placeholder — no shared UI extracted yet.

## What it does

Reserved package for shared OSM editor UI (Catalyst components and related primitives). Component code currently lives in the app; this package is scaffold-only until a later extraction wave.

## Usage

```ts
import { OSM_UI_PACKAGE } from '@osm-editor-kit/osm-ui'

OSM_UI_PACKAGE // → '@osm-editor-kit/osm-ui'
```
