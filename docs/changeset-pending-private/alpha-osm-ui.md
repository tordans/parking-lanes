---
"@osm-editor-kit/osm-ui": minor
---

Pending npm alpha packaging (not in the first publish wave). Documents the kit package as it exists today.

Core features to date:
- Placeholder package reserved for future shared OSM editor UI (Catalyst components and related primitives)
- Package name constant export (`OSM_UI_PACKAGE`) for monorepo references
- TypeScript module scaffold (`package.json` exports, `type-check` script); component code still lives in the app
