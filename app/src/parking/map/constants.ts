export const viewMinZoom = 15

/** Wait after the last viewport change before coverage check / Overpass fetch ([TanStack Pacer](https://tanstack.com/pacer/latest)). */
export const coverageFetchDebounceMs = 400
