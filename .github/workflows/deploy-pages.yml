name: Deploy GitHub Pages

# Pages source must be set to "GitHub Actions" in repo Settings → Pages.
on:
  push:
    branches: [main, master, react-migratoin]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run check-ci
      - run: bun run build
        env:
          OSM_OAUTH_CLIENT_ID: WmTSsQVfDq0vkM1tbLahyohhowiL0AWHFFp5u5BkJe4
      - run: cp app/dist/index.html app/dist/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
